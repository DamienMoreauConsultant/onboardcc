import React, { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { ArrowLeft, CheckCircle2, Download, FileUp, Loader2, UploadCloud, XCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { candidatsApi, type ImportLine } from '@/api/candidats';

export default function CandidatImport() {
  const [, navigate] = useLocation(); const [file, setFile] = useState<File | null>(null); const [report, setReport] = useState<ImportLine[] | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState('');
  const allValid = !!report?.length && report.every((line) => line.statut === 'ok');
  const verify = async () => { if (!file) return; setBusy(true); setError(''); setReport(null); try { setReport((await candidatsApi.verifyImport(file)).lignes); } catch (e: any) { setError(e.response?.data?.error ?? 'Impossible de vérifier ce fichier.'); } finally { setBusy(false); } };
  const execute = async () => { if (!file || !allValid) return; setBusy(true); setError(''); try { await candidatsApi.executeImport(file); navigate('/recruteur/candidats'); } catch (e: any) { setError(e.response?.data?.error ?? 'L’import a échoué. Aucune donnée n’a été enregistrée.'); } finally { setBusy(false); } };
  return <div className="p-6 md:p-8"><Link href="/recruteur/candidats"><Button variant="ghost" className="mb-5"><ArrowLeft className="mr-2 h-4 w-4"/>Retour aux candidats</Button></Link><div className="mb-6"><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Flux site DCC</p><h1 className="text-3xl font-display font-bold">Importer des candidats</h1><p className="mt-2 text-sm text-muted-foreground">L’import CSV est réservé à la création initiale des candidats. Toute correction ultérieure se fait depuis l’écran Recruteur.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_1.3fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><FileUp className="h-5 w-5 text-primary"/>1. Choisir le fichier</CardTitle><CardDescription>CSV UTF-8, exactement 47 colonnes contractuelles.</CardDescription></CardHeader><CardContent className="space-y-5"><a href="/api/candidats/import/template" download="template_candidats_dcc.csv"><Button variant="outline" className="w-full"><Download className="mr-2 h-4 w-4"/>Télécharger le template CSV</Button></a><label className="flex min-h-40 cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed border-primary/25 bg-primary/[.03] p-6 text-center"><UploadCloud className="mb-2 h-8 w-8 text-primary"/><span className="font-medium">{file?.name ?? 'Déposer ou sélectionner un fichier .csv'}</span><span className="mt-1 text-xs text-muted-foreground">Aucune donnée n’est enregistrée avant confirmation.</span><input type="file" accept=".csv,text/csv" className="sr-only" onChange={(e) => { setFile(e.target.files?.[0] ?? null); setReport(null); }}/></label><Button className="w-full" disabled={!file || busy} onClick={() => void verify()}>{busy ? <Loader2 className="mr-2 h-4 w-4 animate-spin"/> : <CheckCircle2 className="mr-2 h-4 w-4"/>}Vérifier le fichier</Button>{error && <p className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{error}</p>}</CardContent></Card><Card><CardHeader><CardTitle>2. Rapport de vérification</CardTitle><CardDescription>{report ? `${report.length} ligne(s) analysée(s)` : 'Le rapport apparaîtra après la vérification.'}</CardDescription></CardHeader><CardContent>{report ? <><div className="max-h-[470px] overflow-auto rounded-lg border"><table className="w-full text-sm"><thead className="sticky top-0 bg-muted"><tr><th className="p-3 text-left">Ligne</th><th className="p-3 text-left">Statut</th><th className="p-3 text-left">Message</th></tr></thead><tbody>{report.map((line) => <tr key={line.ligne} className="border-t"><td className="p-3 font-mono">{line.ligne}</td><td className="p-3">{line.statut === 'ok' ? <span className="flex gap-1 text-emerald-700"><CheckCircle2 className="h-4 w-4"/>OK</span> : <span className="flex gap-1 text-destructive"><XCircle className="h-4 w-4"/>Erreur</span>}</td><td className="p-3">{line.message}</td></tr>)}</tbody></table></div><Button className="mt-5 w-full" disabled={!allValid || busy} onClick={() => void execute()}>{busy && <Loader2 className="mr-2 h-4 w-4 animate-spin"/>}{allValid ? 'Confirmer l’import' : 'Corrigez les erreurs avant de confirmer'}</Button></> : <div className="flex min-h-64 items-center justify-center text-center text-sm text-muted-foreground">La vérification n’effectue aucune écriture.</div>}</CardContent></Card></div></div>;
}